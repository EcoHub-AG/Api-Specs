{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "description": "Summary analysis of the overall impact of preprocessing on document quality.",
  "type": "object",
  "properties": {
    "documentQualityBefore": {
      "description": "Document quality score before any preprocessing.",
      "type": ["null", "number", "string"],
      "minimum": 0,
      "maximum": 1,
      "pattern": "^(0(\\.\\d+)?|1(\\.0+)?)$"
    },
    "documentQualityAfter": {
      "description": "Document quality score after all preprocessing.",
      "type": ["null", "number", "string"],
      "minimum": 0,
      "maximum": 1,
      "pattern": "^(0(\\.\\d+)?|1(\\.0+)?)$"
    },
    "improvementFromPreprocessing": {
      "description": "Calculated improvement in document quality from preprocessing.",
      "type": ["null", "number", "string"],
      "minimum": 0,
      "maximum": 1,
      "pattern": "^(0(\\.\\d+)?|1(\\.0+)?)$"
    },
    "mostImpactfulStep": {
      "description": "Name of the preprocessing operation that had the greatest positive impact on quality.",
      "type": ["null", "string"]
    },
    "receiverNote": {
      "description": "Human-readable note explaining the preprocessing impact for the receiver.",
      "type": ["null", "string"]
    }
  },
  "additionalProperties": false
}
